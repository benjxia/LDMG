name: ldmg
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - auraloss
      - folium
      - tensorboard
      - torch
      - torchvision
      - torchaudio
      - torchaudio-augmentations
      - torchtext
      - lightning
      - numpy
      - pandas
      - scipy
      - torchvggish
      - soundfile