name: ldmg
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - auraloss
      - folium
      - tensorboard
      - torch==2.8.0
      - torchvision==0.23.0
      - torchaudio==2.8.0
      - torchaudio-augmentations
      - torchtext
      - lightning
      - numpy
      - pandas
      - scipy
      - torchvggish
      - soundfile